function lovePhoto(photoID,userID){		var photo_id = photoID;	var user_id = userID;	var base_url = $('#base_url').val();	/*$.post(base_url+'photo/likephoto',{photoID:photoID,userID:userID},function(res){				if(res=='error' || res=='exists'){			unlovePhoto(photo_id,user_id,base_url);		}else{			$('#love'+photoID).text(res);			$('#img'+photoID).attr('src',base_url+'img/loves.png');			$('#img'+photoID).attr('title',res+' people love this.');		}	});*/				if($('#img'+photoID).attr('src') == base_url+'img/loves.png'){								$('#img'+photoID).attr('src',base_url+'img/love.png');			$.post(base_url+'photo/dislikephoto',{photo_id:photo_id,user_id:user_id},function(res){					$('#love'+photoID).text(res);				});					}else{						$('#img'+photoID).attr('src',base_url+'img/loves.png');			$.post(base_url+'photo/likephoto',{photoID:photoID,userID:userID},function(res){						if(res=='error' || res=='exists'){									}else{					$('#love'+photoID).text(res);				}			});					}		}function loveTimelinePhoto(photoID,userID){		var base_url = $('#base_url').val();	var photo_id = photoID;	var user_id  = userID;	$.post(base_url+'photo/likephoto',{photoID:photoID,userID:userID},function(res){				if(res=='error' || res=='exists'){			unloveTimelinePhoto(photo_id,user_id,base_url);		}else{			//$('#lovetl'+photoID).text('You loved this.');			//$('#img'+photoID).attr('src',base_url+'img/loves.png');			$('.like'+photoID).remove();			$('#lovetl'+photoID).append('<img src="'+base_url+'img/loves.png">');			$('#labeltl'+photoID).text(res);		}	});}function unlovePhoto(photo_id,user_id,base_url){	//var base_url = $('#base_url').val();	$.post(base_url+'photo/dislikephoto',{photo_id:photo_id,user_id:user_id},function(res){				//unlovePhoto(photo_id,user_id,base_url);			$('#love'+photo_id).text(res);			$('#img'+photo_id).attr('src',base_url+'img/love.png');			$('#img'+photo_id).attr('title',res+' people love this.');				});	}function unloveTimelinePhoto(photo_id,user_id,base_url){		$.post(base_url+'photo/dislikephoto',{photo_id:photo_id,user_id:user_id},function(res){				//unlovePhoto(photo_id,user_id,base_url);			$('.like'+photo_id).remove();			$('#lovetl'+photo_id).append('<img src="'+base_url+'img/love.png">');			$('#labeltl'+photo_id).text(res);				});	}