var curr_menu = 'challenge_menu';activateMenu(curr_menu);$(document).ready(function(){	var rules = {		    	rules: {					title: {						minlength: 2,						required: true					},					description: {						required: true,					}				}		    };			    var validationObj = $.extend (rules, Application.validationRules);	    		$('#create_challenge').validate(validationObj);});